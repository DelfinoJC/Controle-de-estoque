import ProductFormUser from "./components/ProductForm/ProductFormUser"

function App() {

  // const removeItemOfList = (id: number) => {
  //   setItem(item.filter((i) => i.id !== id));
  // };
  
  return (
    <>
      <ProductFormUser addItem={} />
    </>
  )
}

export default App
